---
spring:
  application:
    name: transactions
  cloud:
    config:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update
security:
  basic:
    enabled: false
management:
  security:
    enabled: false
